# 故障调查 HTML 原型验证记录

日期：2026-09-20。

## 已完成

- `node --check design/fault-investigation/app.js`：通过，JavaScript 语法有效。
- 本机独立无头 Google Chrome 成功加载本地 HTML，生成 `design/fault-investigation/desktop.png`。
- 人工查看 1440 像素宽桌面全页截图：导航、范围栏、调查主线、跨仓库调用链、右侧上下文和输入区显示正常，无明显重叠。
- 新增文件位于独立的 `design/fault-investigation/` 和 `docs/plans/`。未修改 Flutter 实现及已有 maintenance-v2 原型。

## 未完成与原因

本机 Playwright 自带 Chromium 未安装，随后使用已安装的 Google Chrome。Chrome 在沙箱内无法启动，获准使用独立无头浏览器后成功生成桌面截图。

首轮自动交互检查在点击证据时超时：脚本选中了折叠工具卡内不可见的 E02 引用。已经将脚本选择器调整为可见引用，但后续浏览器执行授权被用户拒绝，因此没有继续运行，也没有将未执行项目计为通过。

以下仍待实际浏览器交互验证：

- 审批允许/拒绝、调查停止与继续、迟到回调不复活。
- 证据弹窗、报告切换与 Markdown 下载。
- 新建调查、输入转义、补充方向、附件文件名和历史搜索。
- 项目与数据源页、工单带入，以及 1024 / 390 像素宽布局。
- 全流程浏览器脚本错误与网络请求检查。

随后按用户要求尝试内置浏览器打开原型。内置浏览器的 URL 安全策略拒绝访问该本地 `file://` 页面，并明确禁止通过替代地址、浏览器表面或其他间接方式绕过。本次未继续尝试绕过，因此内置浏览器交互验证也未完成。

`verify.py` 保留了这些检查步骤，当前不能称为全部通过。待允许执行时可使用：

```bash
# 在项目根目录运行，仅使用本机 Chrome 检查本地 HTML。
python3 design/fault-investigation/verify.py
```

## 范围说明

页面逻辑为固定模拟场景，所有工单与接口记录为演示材料。源码节选来自设计阶段静态阅读，没有固定生产部署版本；上传只显示文件名，没有读取文件内容。

尚未验证 Flutter 原生界面、Pi SDK、真实模型、真实日志、业务查询接口、数据库、对象存储或生产排障效果。本次也未执行真实业务读写操作。
