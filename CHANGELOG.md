# 版本日志

## v1.3.5 · 2026-09-24

修复从 macOS 沙盒版（≤1.3.3）升级到非沙盒版后设置与查询历史"丢失"。

- 启动时自动检测旧沙盒容器内的偏好文件，把连接配置、查询历史、GitHub 加速站等设置搬回新位置；新位置已有配置时不覆盖。
- 安全存储中的业务密钥仍有效；个别机器若提示钥匙串授权，选择「始终允许」即可，读不到时重新导入一次密钥文件。
- 已升级 v1.3.4 且设置已空的机器：重新保存一次设置即可，或联系维护者手动搬回。

## v1.3.4 · 2026-09-24

修复 macOS 应用内更新后新 App 无法打开：沙盒下载的文件会被 Gatekeeper 硬性拒绝执行。

- macOS 正式包与调试包不再启用 App Sandbox（内部工具，ad-hoc 签名分发）；应用内下载的 DMG 不再带隔离属性，拖入「应用程序」即可直接打开。
- 无其他功能变化。

## v1.3.3 · 2026-09-23

验证版本：用于从 v1.3.2 沙盒正式包实测应用内自动更新修复效果，无功能变化。

## v1.3.2 · 2026-09-23

修复 macOS 应用内更新卡在下载 100%：正式包启用系统沙盒，直接写「下载」目录与 spawn `open` 进程均被拒绝。

- macOS 更新包改存应用支持目录（沙盒内可写），打开 DMG 改用系统 LaunchServices（沙盒允许）。
- 拉起安装增加 30 秒超时兜底，异常时错误提示带具体原因，进度弹窗不再无限停留。

## v1.3.1 · 2026-09-23

验证版本：用于实测 v1.3.0 的应用内自动更新流程（加速站配置、下载与拉起安装），无功能变化。

## v1.3.0 · 2026-09-23

「检查更新」升级为应用内自动更新：可配置 GitHub 加速站，启动自动识别新版本，应用内下载并拉起安装。

- 设置弹窗新增「GitHub 加速站（可选）」：填入加速站前缀（如 `https://ghproxy.net/`）即时代理更新下载，留空直连 GitHub，输入即保存。
- 启动后静默检查最新 Release，仅发现新版本时弹窗；设置内「检查更新」手动入口保留。
- 发现新版本按平台自动匹配 Release 附件，应用内流式下载（进度条、可取消），失败或取消自动清理残留文件。
- 下载完成拉起安装：Android 调系统 APK 安装器；macOS 打开 DMG 提示拖入「应用程序」；Windows 解压便携 ZIP 并打开所在文件夹，提示关闭旧版后运行新程序；iOS 未签名 IPA 无法自安装，维持浏览器跳转发布页。
- Android 新增 `REQUEST_INSTALL_PACKAGES` 权限；新增 archive、open_filex 依赖；全量测试 55 项。

## v1.2.0 · 2026-09-23

新增单据 PDF 上传修复与检查更新，重构响应式布局与报文解析。

- 新增「上传修复」（仅单据 PDF 页）：选择修复用 PDF 后，先经内部备份引导把当前文件保存到本地留存，再依次确认单据信息（单据号、CRM 流水号、业务月份与 OBS 目标参数）、确认已备份、红色最终确认，三级确认全部通过才调用 `/test/look` 上传；成功后自动重新读取 OBS 预览最新文件。上传走独立 multipart 通道，不经过只读白名单与缓存，接口记录只写字段参数与文件名大小，不记录文件内容。
- 新增「检查更新」（设置弹窗内）：查询 GitHub 最新 Release 并与当前版本比较，发现新版本展示更新说明并可用系统浏览器前往下载。
- PDF 阅读器：宽屏（≥900）默认展开缩略图导航、窄屏默认收起，可手动切换并记住选择；默认适应整页，手动缩放后显示当前态。
- 响应式布局统一断点（700 移动/桌面分界），桌面左侧查询结果栏随屏宽加宽。
- 新增报文结构化解析：报文文件按字段节点树展示，聚焦查看无需重新请求后台。
- 接口请求日志按会话持久化为 JSONL（系统应用数据目录 `request-logs/`），仅记录调用方挑选的字段，不记录认证头与密钥。
- 全量测试 47 项。

## v1.1.0 · 2026-09-18

新增数据表查询与全量报文加载。

- 新增「数据查询」：按 WO_INFO / WO_BIZ_INFO / WO_SIGN_INFO / WO_PIC_INFO 四张表直查，复用既有只读接口，不新增后端接口。
- 查询条件支持单据号、受理流水、业务月份和图片序号（仅图片表）；受理流水须搭配业务月份。
- 查询结果以表格展示，点击行查看完整字段，支持逐字段或整份 JSON 复制。
- 报文文件列表新增「全量报文」：选中后调用 `/agapi/biz/downloadBiz`（仅传单据号），JSON 内容由报文解析器展示，可保存为文件。
- 接口记录完整覆盖数据查询与全量报文请求；全量测试 34 项。

## v0.0.1 · 2026-09-13

首次发布无纸化维护中心四端 Release 正式包。

- 原生只读查询：单据号、受理流水、手机号，单月与最多 12 个月范围查询。
- 查询进度、取消保留结果、响应缓存、最近 30 次本地搜索历史。
- 按 WO_INFO 表字段展示详情，隐藏无数据普通字段，保留 EXT1～EXT10。
- 桌面详情/PDF 分栏，移动端独立搜索与底部分类。
- PDF 连续渲染、缩略图和缩放；文本、图片与 PDF 应用内全屏。
- JSON/XML 递归解析专注、折叠和关键字/Key 搜索；签字和图片连续画廊。
- 系统安全存储业务密钥，文件下载，实际接口请求/响应记录。
- Windows 自签名便携 ZIP、macOS ad-hoc 签名 DMG、Android 发布签名 APK、iOS 未签名真机 IPA。

签名限制与安装步骤见 README；Release 构建不代表 App Store 上架或系统默认信任。

- 四端统一由 GitHub Actions 构建；推送版本标签后自动测试、构建并发布完整 Release。
